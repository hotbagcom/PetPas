import 'package:flutter/material.dart';

class MainColors {
  final backGroundColor = const Color.fromRGBO(248, 248, 248, 100);
  final primaryColor = const Color.fromRGBO(34, 172, 202, 100);
  final thirdColor = const Color.fromRGBO(110, 22, 124, 100);
}

class Paddings {
  static const profilePagePadding = EdgeInsets.symmetric(horizontal: 20);
  static const profilePageColumnPadding = EdgeInsets.all(20);
}
